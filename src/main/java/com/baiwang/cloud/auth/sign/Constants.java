package com.baiwang.cloud.auth.sign;

public interface Constants {
    interface Charset {
        String UTF_8 = "utf-8";
    }

    interface Query {
        String PARAMETER_SEPARATOR = "&";
        String PARAMETER_VALUE_SEPARATOR = "=";
    }
}
